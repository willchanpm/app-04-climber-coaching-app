# Use legacy peer deps to resolve React 19 compatibility with react-leaflet
# react-leaflet hasn't updated its peer dependency requirements yet,
# but it works fine with React 19
legacy-peer-deps=true

